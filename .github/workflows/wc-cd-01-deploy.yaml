on:
  workflow_call:
jobs:
  deploy-ec2:
    name: "🖥️deploy"
    runs-on: ubuntu-latest
    steps:
      - name: Temporary Save
        env:
          DOCKER_IMAGE_TAG: ${{ env.IMAGE_TAG }}
        run: echo "cd 워크플로우 작성 중 $DOCKER_IMAGE_TAG"
