spring:
  application:
    name: api-backoffice
  profiles:
    active: local
    include: datasource, openapi, logging

server:
  port: 10002

---
spring:
  config:
    activate:
      on-profile: dev
    import: aws-secretsmanager:/secret/pay200
